# 로그/문서화 규칙 (확정)
> 2026-06-02 확정

---

## 핵심 원칙

로그는 기록 수단이 아니다.
**추측으로 수정하는 일을 구조적으로 없애는 장치다.**

증거 없이 수정 → 틀림 → 반복 → 시간 낭비 → 또 다른 버그.
이 반복의 근본 원인은 "증거 없이 하나만 보고 판단하는 것"이다.

로그가 있으면 전체 흐름이 보인다.
전체 흐름이 보이면 추측할 필요가 없다.
추측하지 않으면 틀리지 않는다.

> 제1규칙과 같은 말이다:
> "가능한 모든 정보를 사용하여 추론하고 결론을 도출한다."
> 로그 = 그 "모든 정보"를 만드는 장치.

**증거 수집 → 원인 특정 → 수정. 이 순서를 절대 바꾸지 않는다.**

---

## 규칙 1 — 소통으로 도출한 계획은 문서로 저장한다

대화를 통해 방향·계획·결정이 나오면 반드시 `.md` 파일로 저장한다.
저장하지 않으면 다음 세션에서 같은 내용을 처음부터 다시 논의하게 된다.

- 저장 위치: 해당 프로젝트 루트
- 저장 후 메모리(MEMORY.md)에 위치 기록
- 예시: `PROJECT_PLAN.md`, `LOGGING_RULES.md`

---

## 규칙 2 — 실행은 100% 로그로 기록한다

실행 결과를 기록하지 않으면 두 가지 문제가 발생한다.

### 문제 1 — 세션 연속성
로그 없으면:
```
매 세션마다 "어디까지 했지?" 확인 필요
→ 이미 된 작업을 다시 시킴
→ 사용자 반복 작업, 시간 낭비
```
로그 있으면:
```
씨드 실행 → ✅ 완료 (2026-06-02), storeKey: sk_incheon_daa4...
현재 위치 → Phase 1, 1번 Socket.IO 작업 중
→ 멈춘 곳에서 바로 이어서 시작
```

### 문제 2 — 버그 원인 즉시 특정
로그 없으면:
```
"주방 출력 안 됨" → 원인 모름 → 추측 수정 → 반복
"주문 수신 느림" → 원인 모름 → 하나씩 테스트
```
로그 있으면:
```
"주방 출력 안 됨"
→ 서버 audit_log: ORDER_CREATED 있음 → 주문은 도달
→ POS _failedPrints 배지: 3회 재시도 실패, COM 포트 오류
→ 원인: 프린터 연결 끊김 → 즉시 해결

"주문 수신 2분 지연"
→ git log: 9dd005c 배포 후 socket room 방식 변경됨
→ 앱에 STORE_KEY 없음 확인
→ 원인 특정 → 즉시 해결
```

---

## 실행 로그 기록 기준

| 항목 | 기록 방법 |
|------|---------|
| 작업 완료 | 체크박스 즉시 업데이트 `[ ]` → `[x]` |
| 생성된 값 (키, ID, URL) | 메모리/문서에 즉시 저장 |
| 커밋 | 해시 + 내용 기록 |
| 빌드 결과 | 성공/실패 + 결과물 경로 |
| 서버 배포 | 커밋 해시 + 배포 시각 |
| 오류 발생 | 오류 메시지 + 원인 + 해결 방법 |

---

## 서버 구현 시 audit_log 필수 항목

치킨 프로젝트 서버 구현 시 처음부터 아래 이벤트 로깅 구조를 잡는다:

```
ORDER_CREATED       → 주문 생성 성공
ORDER_STATUS_CHANGED → 상태 변경
PAYMENT_APPROVED    → 결제 승인
PAYMENT_CANCELLED   → 결제 취소
PRINT_FAILED        → 출력 실패 (3회 재시도 후)
SOCKET_CONNECTED    → POS/앱 소켓 연결
```

로그가 있으면 "어디서 끊겼는지" 1단계로 좁힌다.
로그가 없으면 전체를 다시 테스트해야 한다.
